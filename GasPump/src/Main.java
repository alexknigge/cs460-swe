import Devices.GasPumpUI;

import static javafx.application.Application.launch;

// Main
public class Main {
    public static void main(String[] args) {
        GasPumpUI.launch(GasPumpUI.class, args);
    }
}
